import { Box, Text } from "@chakra-ui/react";
import { Link } from "@remix-run/react";

export const index = () => {
  return (
    <Box>
      <Text>Hello</Text>
    </Box>
  );
};

export default index;
